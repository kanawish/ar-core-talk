material {
    name: "Custom material",
    parameters: [
//        {
//            type: "sampler2d",
//            name: "andyColor"
//        },
        {
            type: "sampler2d",
            name: "metalx"
        }
    ],
    requires: [
        "position", "uv0"
    ],
    shadingModel: "lit",
}

fragment {
    void material(inout MaterialInputs material) {
        prepareMaterial(material);
        // float3 texSample = texture(materialParams_andyColor, getUV0()).rgb;
        float3 metalx = 1.0 - texture(materialParams_metalx, getUV0()).rgb;

        // Fade between the base color and a gold-ish metal
        // float3 colorA = texSample;
        float3 colorB = float3(1.00, 0.85, 0.57);
        material.baseColor.rgb = metalx;    // mix(colorA, colorB, metal);

        //material.metallic = metalx;
        //material.roughness = 1.0 - metalx;
    }
}